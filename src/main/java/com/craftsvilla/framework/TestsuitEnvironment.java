package com.craftsvilla.framework;

public class TestsuitEnvironment {

	public static void setenvirnment()

	{
		//String browsername=EnvironmentBo
		/*PropertyReader read = new PropertyReader();
		//EnvironmentBo env = new EnvironmentBo();
		
		String os = read.getPropertyValue("os");
		
		String browsers = read.getPropertyValue("browsers");
		List<String> browserslist = new ArrayList<>();
		List<String> oslist = new ArrayList<>();
		StringTokenizer ostoken = new StringTokenizer(os, ",");
		for (int i = 0; i <= ostoken.countTokens(); i++) {
		
			oslist.add(ostoken.nextToken());
		
		}
		
		StringTokenizer browser = new StringTokenizer(browsers, ",");
		while (browser.hasMoreTokens()) {
		
			browserslist.add(browser.nextToken());
		
		}
		for (int i = 0; i < oslist.size(); i++) {
			System.out.println(oslist.get(i));
		}
		for (int i = 0; i < browserslist.size(); i++) {
			System.out.println(browserslist.get(i));
		}
		EnvironmentBo.setAllBrowsers(browserslist);
		EnvironmentBo.setAllOS(oslist);
		*/ }
}
